# 上游适配与部署验收（1.5.39）

目标：FlashyyL/oai-adversarial-plugin 的 main，基于
`b1a959dd5a7b06645b56c8614e0c2b5a2031547c`。

## 兼容性

- 保留上游可配置 IANA 时区、可接受 state 长度策略和状态镜像功能。
- 保留模型优先级、同优先级 FIFO、优先级让出后的已用预算，以及探测休眠设置。
- 账号续期任务保留独立目标与到期周期，在通过休眠/优先级检查后、实际发请求前持久化一次性尝试记录。
- 保留上游代理/传输错误不损伤账号健康的保护；额度错误优先分类，429 临时退出候选池但不记为降级。
- 已核实的当前启用账号才出现在健康表中；禁用、删除及无法核实的历史记录不会冒充当前账号。临时限流/鉴权错误的启用账号仍可查看。

## 验证范围

- Windows/Linux 全量 Go 测试、go vet。
- Linux 账号清单、休眠、优先级队列、额度、续期和来源守卫 race 测试。
- 55 项管理页面 Node 测试，4 项镜像页面 Node 测试。
- 实际 CPA 7.3.7 二进制配合模拟上游验证 HTTP/SSE/WebSocket、来源隔离及 429 自动切换。
- 账号清单测试涵盖禁用、删除、暂时不可用、读取失败和重新启用。

集成测试中的配置型 API Key 不属于该宿主的实体 auth-file 清单，不要求它出现在账号表中；票据归属通过来源确认计数和真实请求/响应行为验证。

## 发布边界

仅更新公网 CPA 插件，保留原配置、账号文件和历史状态；部署前备份，失败回滚。
不调用代理供应商取 IP 接口，不启用新账号。版本仍为 1.5.39，由构建 SHA-256 和 Git 提交区分本次适配构建。
部署结果记录在服务器 `/opt/runteam-plugin-build/upstream-validation-20260921/`。
