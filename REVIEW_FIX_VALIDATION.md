# PR #1 审查修复历史验收（.13）

版本：`1.5.37-agentease.13`。分支：`codex/fix-state-expiry-reprobe`。
本文件保留该历史构建的实际测试记录，不作为后续 1.5.39 构建的验收结论。
对应审查：<https://github.com/AgentEase/oai-adversarial-plugin/pull/1#pullrequestreview-5260743518>。

## 六项审查意见

1. `probe_pending`：重启恢复时保留待重探、到期及该周期的尝试标记，同时兼容旧探测异常状态迁移。
2. 续期排队：普通模型任务、账号 A 任务和账号 B 任务分别入队，只有同模型同目标账号才去重；统一 FIFO 串行执行，任务不会改用全局选号。
3. 前端测试：新增空账号、到期、待重探、业务异常、冷却、健康、续期已尝试、守卫统计及提示清理测试。
4. 验收版本：`SESSION_GUARD_VALIDATION.md` 明确是 `.11` 历史验收，不冒充新版本验证。
5. 长度说明：代码注释、错误提示及运行策略说明统一为接受 292/332，且不将长度等同于模型能力。
6. 国际化：账号区表头、状态、守卫统计、来源徽标及提示接入既有四语言机制，并测试语言切换。

## 到期主动检测

- 每 30 秒扫描账号，不依赖业务流量，提前预备窗口为 0 时仍扫描。
- 只有账号路由与自动选号探测均启用才执行；停止全部、暂停、禁用及凭证冷却仍生效。
- 以 AuthID、模型族和本次到期时间标记周期，执行前核对是否仍需要续期。
- 每周期最多一次自动上游尝试，尝试标记先写入持久化文件，失败后不自动循环。
- 快照收集和写入使用同一写锁，防止旧快照覆盖新的尝试标记。
- 若保存失败则不发请求；若写入成功后、发送前崩溃，恢复后也不自动补发，可手动重探。
- 新票据形成新周期；已续期后留在队列里的旧周期任务会跳过。

## 已执行验证

- Windows / Linux：`go test -count=1 ./...`、`go vet ./...` 通过。
- Linux：持久化、到期扫描、排队、来源守卫相关 race 测试通过。
- Linux 实际 CPA 7.3.7 二进制副本配合模拟上游：HTTP、SSE、WebSocket、鉴权、覆写及来源守卫对照测试通过。
- Node：`node --test src/web/index.test.cjs`，36/36 通过。
- 模拟上游测试确认：请求发送前尝试标记已在磁盘；失败上游只收到一次请求；磁盘失败收到零次请求。
- `git diff --check` 通过。

本轮验证未更新生产容器、未使用真实账号发起模型探测、未调用代理供应商。
