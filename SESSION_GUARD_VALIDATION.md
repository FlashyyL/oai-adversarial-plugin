# 会话票据来源守卫历史验收（2026-09-20，仅限 .11）

版本：`1.5.37-agentease.11`。分支：`codex/account-scoped-state-ttl`。

本文件只记录 `.11` 当时的构建、部署和测试结果，不是当前 PR 最新构建的
验收记录。下文生产模式、备份路径和 SHA-256 均属于该历史版本；后续版本的
续期、持久化和前端审查修复应以相应提交的测试结果为准，不沿用本文件结论。

## 已验证的改善

使用公网实例的 CPA 7.3.7 二进制副本和本地模拟上游，分别开启 `off` 与
`enforce`，实际捕获上游请求和下游响应，不使用真实账号凭证。

| 场景 | off 对照 | enforce |
| --- | --- | --- |
| 同账号跨模型，HTTP | 原票据双向转发 | 请求头彻底移除，响应票据清除 |
| 同账号跨模型，SSE | 原票据双向转发 | 请求头彻底移除，响应票据清除 |
| 跨账号同模型，HTTP | 原票据双向转发 | 请求头彻底移除，响应票据清除 |
| 跨账号同模型，SSE | 原票据双向转发 | 请求头彻底移除，响应票据清除 |

结果为 4/4 对照复现串用、4/4 守卫阻止串用。此结果证明隔离行为改善，
不证明能够解除上游账号降级或提升模型能力。

本轮测试修复：

- 用量回调原先可绕过来源检查，把未知或外来票据标记成当前账号健康票据。
- CPA 响应钩子可能缺少实际选号元数据，现通过可信的宿主请求 ID 关联，
  15 分钟过期、最多 4096 条，显式响应账号优先，不从客户端字段猜测账号。
- CPA 请求头合并可能丢失删除指令，现保留兼容空值标记；真实链路验证
  上游完全没有该请求头，而非仅将其内容置空。

## 自动测试

- Windows 与 Linux `go test -count=1 ./...`：通过。
- Windows 与 Linux `go vet ./...`：通过。
- Linux 来源守卫相关 `go test -race`：通过。
- 真实 CPA 集成：Responses、Chat Completions、SSE、WebSocket、模型观测、
  请求覆写、管理接口鉴权及来源守卫对照测试全部通过。
- Dashboard 内嵌 JavaScript 语法检查、`git diff --check`：通过。

## 生产部署与烟雾测试

- 公网容器 `cli-proxy-api-public` 已加载 `.11`，来源守卫模式为 `observe`。
- 保留原有账号路由、强制覆写、探测能力、拒绝降级及自动预备设置。
- 历史账号健康状态、200 条审计记录和探测计数成功恢复。
- 容器运行正常，管理页面可用，没有观察到插件加载错误。
- 一次正常业务入口请求：`gpt-6-astra`、`low`，HTTP 200，实际返回
  `gpt-6-astra`，4.05 秒完成，`37 × 43` 回答 `1591`，使用当前账号票据。
- 此次请求未观测到新响应 state，不把已有 332 字节票据误报为本次新签发。
- 没有调用代理供应商、提取 IP、指定固定测试账号或增加主动探测任务。

生产备份：
`/opt/cliproxyapi-public/backups/session-guard-20260920-120937`

部署文件 SHA-256：
`4e46dbc5af67273ba2fbbc781d80341699314a4af552894a03c4022941d205d1`

脱敏部署和烟雾测试记录位于服务器：
`/opt/runteam-plugin-build/session-guard-20260920/`。

## 边界与后续判断

生产目前记录来源并防止未知票据直接污染账号池，尚未启用异账号票据强制
剥离。WebSocket 仍受宿主响应头 ABI 限制，只支持请求侧防串用。

一次正确回答仅验证服务链路，不能作为模型能力提升的结论。后续是否启用
`enforce`，应根据真实异账号/异模型回带记录、误判情况和请求完成率判断；
模型能力需要固定题集、相同账号与请求参数的重复对照评估。
