# 额度分类与自动切换验收：1.5.39

版本号对齐上游 main 的 `1.5.39`（核对提交 `b1a959dd5a7b06645b56c8614e0c2b5a2031547c`），不使用分支版本后缀。PR 功能变更保持独立。

## 问题与修复

旧用量回调先判断 state 长度，后判断 429，可能将额度错误中的 356 字节 state
归为业务降级；普通 429 又不让账号进入冷却，导致重复选择。所有账号拒绝还共用
“降智已拦截”标签，掩盖了鉴权和临时失败。

- 优先识别额度错误码、HTTP 429 和限流，读取 Retry-After / 重置时间；缺省等待 60 秒或 5 分钟。
- 暂时跳过受限账号，继续选择宿主给出的健康候选；不绕过优先级、禁用或候选范围。
- 冷却到期自动重新参与路由，无需手动检测。旧票据重放及成功探测不清除额度冷却。
- 没有选中可用替补时返回 429、类型和 Retry-After，不显示为模型降级。
- HTTP、SSE 额度错误与 state 判定分离，鉴权及临时失败使用独立响应类型和徽标。
- 旧快照中混有 429 的长度异常证据迁移为待复核，不直接假定健康；保留真实业务异常。

## 验证

- Windows/Linux 全量 Go 测试与 go vet 通过。
- Linux 额度分类、旧快照迁移、续期相关 race 测试通过。
- 前端 Node 测试 37/37 通过。
- 使用真实 CPA 7.3.7 二进制及两个模拟账号：A 返回 429 和 356 字节 state，
  同一个客户端请求自动切 B 成功；第二个客户端请求继续使用 B。
- 此场景在来源守卫 off/enforce 下均通过，每组只有一次 A 失败和两次 B 成功，无探测调用。
- 原有 HTTP/SSE/WebSocket 与来源守卫集成测试通过。

模拟测试验证了实际宿主重试链路，没有消耗真实账号额度或提取代理。
