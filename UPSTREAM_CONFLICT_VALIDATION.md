# 上游冲突整合（2026-09-21）

生产先部署 `5e3c85c`（1.5.39），仅包含已验证的账号分组 UI。
生产校验确认 2 个账号、3 条模型记录，配置未变，旧二进制已备份。

本次整合基于上游 `48b3c16`，版本跟随上游 **1.5.48**，不使用 fork 后缀。
整合构建与生产现有构建分开；未经单独验证，不以解决冲突为由替换生产。

## 整合策略

- 保留上游凭证级绑定、账号独立 active/candidate、响应票据真实学习结果、账号面板和时区设置。
- 保留本 PR 的额度优先分类、同候选层自动切换、过期不等于降级、每周期一次续期、来源诊断和账号健康分组。
- 上游移除账号冷却与本 PR 目标冲突：保留路由层有限冷却，不修改 CPA 账号启用状态。探测沿用上游的独立账号可用性规则。
- 续期将指定 AuthID 解析到凭证绑定槽位；支持 highest-priority/all-accounts，成功结果可直接被该账号业务请求使用。
- 不通过旧 AuthID 健康记录覆盖新凭证槽位；未知凭证不借用其他账号 state。
- 保留上游测试，调整与本 PR 明确行为冲突的断言（路由冷却与健康面板保留），不跳过安全隔离测试。

## 验证

- Windows 全量 Go 测试与 go vet 通过。
- 管理面板 Node 测试 66/66 通过。
- 新增全账号模式下续期写入正确凭证槽位、其他账号/全局槽位不受影响、业务可使用新票据的回归测试。
- Linux 全量 Go 测试、go vet、账号/续期/额度/来源/凭证隔离专项 race 测试通过。
- 实际 CPA 7.3.7 + 模拟上游 HTTP/SSE/WebSocket、未知身份清除请求票据、来源守卫及 429 自动切换测试通过。
- 额外覆盖凭证级槽位的到期续期失败不误判业务降级；真实业务异常继续拦截。
- API-key 集成夹具不在宿主 auth-file 清单，验证身份未知时禁止全局兜底；正向凭证绑定和续期槽位由宿主回调测试覆盖。
- 镜像面板 4/4 测试通过（与管理面板合计 70 项）。
- 1.5.48 未部署生产；本验收不替代该版本的生产验证。
