# AgentEase 可视化配置

CPA 插件管理 → O/对抗插件 → 配置字段。由 AgentEase 提供初始实现，维护者追加兼容性与策略修复。
声明可编辑顶层字段，兼容 CPA 7.3.7 的字段编辑器。

本 PR 提供 26 个配置字段，保留上游时区与可配置长度策略。账号路由开启后，
票据与 TTL 按 AuthID 和模型隔离；`session-guard-mode` 支持 off/observe/enforce，
`session-provenance-ttl-minutes` 控制短期来源记录（默认 60 分钟）。

额度/限流先于 state 长度分类。受限账号暂时退出候选池，按 Retry-After/重置时间
恢复；没有可用替补时返回 429。到期票据每 30 秒主动扫描，每账号模型每周期最多
一次自动续期，执行前保存尝试记录，重启不重复；保存失败不发请求。
扫描独立于提前预备窗口，但遵守上游休眠、停止、暂停、禁用；额度冷却期间不创建到期续期任务。
任务继续使用上游模型优先级，同优先级 FIFO；不同账号不会复用同一续期任务。

示例：
```yaml
experimental-account-routing: true
session-guard-mode: observe
session-provenance-ttl-minutes: 60
```

推荐仅业务观测配置：

```yaml
operation-mode: business-only
override-policy: preserve-healthy-client
override-models: [gpt-6-astra]
```

此模式不需要固定账号或静态 state，不会启动探测。没有本账号有效基线时清除客户端票据并不注入；
真实业务流量观测到模型一致、符合配置长度策略的 state 后存入基线。
长度规则是本地策略，不证明账号正常或模型能力。

`probe` 模式支持自动选号或显式凭证路径。启用能力不等于开始任务；
开始/停止仍在探测控制台控制。生产使用前配置已有代理，不能填写取 IP API。

`probe-account-mode: all-accounts` 为所有未禁用、可用的 Codex 认证文件分别维护票据；`highest-priority` 只维护最高优先级账号；`fixed` 按完整文件路径精确绑定宿主账号。固定文件无法匹配或宿主认证回调不可用时显示不可用，绝不猜测身份。

每个账号/模型任务及其所有重试始终锁定该账号。探测沿用上游的串行间隔和预算，不因路由冷却修改 CPA 启用状态。业务路由保留有限冷却，区分额度、鉴权与业务异常；到期自动续期每周期最多一次。账号消失或被 CPA 禁用时结束任务，不能借用另一个账号完成。429、代理和传输错误不作为账号降级证据。Token 不写入日志。
插件每 10 秒及读取面板状态时同步 CPA 已启用账号清单和凭据归属，停止或关闭自动预备也不阻止清单同步；最高优先级候选变更且身份绑定可用时触发一轮探测。同步凭据本身不发送模型请求。highest-priority 首次扫描仅建立快照；all-accounts 在原自动预备已开启时补齐尚无基线的账号/模型组合，按组合节流并走统一串行队列。预备窗口为 0、全局停止或暂停时遵守原有门禁，不因账号切换恢复自动探测。

字段包括运行模式、覆写策略、模型数组、凭证/代理文件路径、TTL、预备窗口、
串行间隔、超时、每出口和每轮预算、失败阈值与冷却时间。数组填写 JSON，
例如 `["gpt-6-astra"]`。表单不提供 Token、代理密码或静态 state 输入。

已设置的顶层字段优先于旧 `turn-state-override` YAML；删除字段后恢复继承。
旧环境的表单空值表示未设置别名，不代表底层配置为空。探测控制台的
`runtime-settings.json` 对预备窗口、间隔及出口设置仍有最终优先权。
降智拒绝开关继续在探测控制台管理；请求目标时区可在请求记录的请求设置中选择/输入并保存（持久化后优先于 YAML），也可由 `timezone` 配置（默认 `America/Los_Angeles`），非法 IANA 名称拒绝应用；与面板显示及每日休眠的 UTC+8 无关。

本版本修复关闭探测且静态值为空时误报“覆写：配置错误”的校验。
升级后应同时检查 `/v0/management/plugins` 返回非空 `config_fields`、插件
摘要 `turn_state_override.error` 为空、探测关闭及业务观测记录，不能只检查版本号。

## 可配置长度策略

默认兼容列表为 `[292, 332]`，这是可替换的经验默认值，不按 Team、个人或其他套餐名称自动判断。不同账号可能共用一个显式验收列表；应根据实际证据配置，长度匹配仍不证明账号或模型能力。

```yaml
accepted-state-lengths: [292, 332]
```

也可在 `turn-state-override.accepted-state-lengths` 中配置；顶层别名优先。接受 1–32 个不同整数，每个 1–4096，空数组、重复值、浮点数和字符串均拒绝。正常业务、探测验收和账号路由共用该配置；长度不能证明客户端 State 的账号归属；管理页根据 API 策略显示，不另写固定长度规则。

账号健康证据最多有效一小时，普通成功但无 State 的请求不续期证据；同一规则用于运行和快照恢复。旧快照缺少健康证据时间时先按未知处理。成功请求重置连续失败计数，代理/传输错误及 429 不处罚账号；真正的 401/403 仍按认证异常处理。路由默认关闭，启用后也只在宿主提供的同优先级候选中选择。

## 账号绑定与迁移

票据按 CPA AuthID、真实凭据身份及模型隔离。真实身份优先取宿主选中的 JWT 签发方/主体与工作区；身份字段不完整或为不透明 Token 时采用 Token 摘要，无法确认原身份时停止使用该认证项的票据。同名文件替换成另一账号或不透明 Token 更换后，该认证项在当前 CPA 进程内隔离，必须重启 CPA 才能重新绑定；相同主体与工作区的 JWT 刷新保留原作用域。所有标识只保存哈希，不保存原始 Token。文件重命名或重新导入导致 AuthID 改变时视为新作用域，不自动迁移旧票据。持久化保存独立双槽及暂停状态；旧无归属票据保留但隔离，全局静态 value 不回灌到账号请求。客户端票据不凭长度保留，切换账号后只使用新账号自己的票据，没有时清除或置空请求头（兼容旧宿主的二次合并）。

后台先通过 `host.auth.list` 列举稳定 ID/索引和状态，指定账号探测通过 `host.auth.get` 获取对应认证 JSON。业务请求使用 CPA after-auth 的 `Metadata.selected_auth_id` 和 `selected_auth_index`，核对清单后通过 `host.auth.get` 读取对应凭据。此回调在执行器设置上游 Authorization 之前，请求头仍可能来自客户端，不能据此认定账号。宿主不提供所选凭据的不可变快照，因此进程内固定首次确认的身份，检测到同一 ID 换身份后保持隔离，不自动重新启用。不要在同一 CPA 进程运行期间用同名文件替换成不同账号；应停止 CPA 后更换并重启。响应只查该请求发出时的绑定，不重新读取当前认证文件猜归属；请求记录已淘汰或缺绑定时不捕获、不回灌。Usage 缺少请求绑定，只用于原有账号健康统计，不捕获票据。旧宿主缺少身份元数据或 Host Auth 凭据读取能力时明确记录账号未识别并停止票据注入。完整宿主兼容仍须针对实际 CPA 版本做隔离集成验证。

管理面板先显示模型汇总行，至少一个已启用账号具有有效基线即显示可用，否则显示不可用；不检测模型单独标记。默认折叠账号详情，展开后显示邮箱及各自票据，暂停/恢复/单次探测携带该行 target。邮箱默认隐藏为 ***，可切换显示，切换语言及轮询保持显隐和展开状态。CPA 禁用的账号不显示，重新启用后重新出现，原票据保留但不跨账号。本 PR 额外保留实验性健康路由区，按账号分组并分别计数账号与模型记录；各模型 TTL 独立。保留有限路由冷却，不提供冷却旁路开关。代理出口轮休/冷却及串行预算不变。公开镜像仅展示各模型的可用账号数量，不发送账号标识，多账号时不把其中一张票据的倒计时当作全部账号的倒计时。镜像协议增加可选 accounts_total/accounts_ready 字段；发布时先更新接收端。
